# Components Module

Shared UI components used across all business modules.

## Standards

- Props must be typed.
- Public component props should include JSDoc descriptions.
- Components must remain tenant-agnostic; authorization happens in route/page layers.
